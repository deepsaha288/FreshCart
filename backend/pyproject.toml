[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "freshcart-backend"
version = "0.1.0"
description = "FreshCart - Grocery Ordering Platform Backend"
requires-python = ">=3.12"
